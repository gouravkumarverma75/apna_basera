import{useState}from"react";import{useNavigate}from"react-router-dom";export default function SearchPanel(){const[v,setV]=useState({location:"",budget:15000,room:""}),nav=useNavigate();const go=()=>{const q=new URLSearchParams();if(v.location)q.set("location",v.location);if(v.budget)q.set("maxRent",v.budget);if(v.room)q.set("roomType",v.room);nav("/explore?"+q)};return <div className="search-panel"><label>📍 WHERE?<input value={v.location} onChange={e=>setV({...v,location:e.target.value})} placeholder="College, area or city"/></label><label>₹ BUDGET<select value={v.budget} onChange={e=>setV({...v,budget:e.target.value})}><option value="7000">Up to ₹7,000</option><option value="8000">Up to ₹8,000</option><option value="10000">Up to ₹10,000</option><option value="15000">Any budget</option></select></label><label>🛏 STAY TYPE<select value={v.room} onChange={e=>setV({...v,room:e.target.value})}><option value="">Any room</option><option>Single</option><option>Double</option><option>Shared</option></select></label><button className="btn search-btn" onClick={go}>Find My Basera →</button></div>}