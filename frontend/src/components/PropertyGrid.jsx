import PropertyCard from"./PropertyCard.jsx";export default function PropertyGrid({properties,loading}){if(loading)return <div className="loading"><div className="loader-dot"/>Finding your basera...</div>;if(!properties.length)return <div className="empty"><div>⌕</div><h3>No basera found.</h3><p>Try increasing your budget or searching a nearby area.</p></div>;return <div className="property-grid">{properties.map(p=><PropertyCard key={p.id} property={p}/>)}</div>}