import{createContext,useContext,useState}from"react";import{getSavedProperties,saveProperties}from"../utils/localStorage.js";const C=createContext();export function SavedProvider({children}){const[saved,setSaved]=useState(getSavedProperties);const toggleSaved=p=>setSaved(a=>{const n=a.some(x=>x.id===p.id)?a.filter(x=>x.id!==p.id):[...a,p];saveProperties(n);return n});return <C.Provider value={{savedProperties:saved,toggleSaved,isSaved:id=>saved.some(x=>x.id===id)}}>{children}</C.Provider>}export const useSavedContext=()=>useContext(C);