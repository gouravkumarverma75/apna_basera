import Navbar from"../components/Navbar.jsx";import PropertyCard from"../components/PropertyCard.jsx";import useSaved from"../hooks/useSaved.js";import{useRecentlyViewed}from"../context/RecentlyViewedContext.jsx";export default function Dashboard(){const{savedProperties}=useSaved(),{recentlyViewed}=useRecentlyViewed();return <><Navbar/><main className="dashboard container"><span className="eyebrow">YOUR SPACE</span><h1>Welcome back 👋</h1><p className="muted">Keep your favourite baseras close.</p><div className="dash-stats"><div><b>{savedProperties.length}</b><span>Saved</span></div><div><b>{recentlyViewed.length}</b><span>Recently Viewed</span></div><div><b>0</b><span>Enquiries</span></div></div><section className="section compact"><h2>Saved Baseras</h2>{savedProperties.length?<div className="property-grid">{savedProperties.map(p=><PropertyCard key={p.id} property={p}/>)}</div>:<div className="empty"><h3>Your shortlist is waiting.</h3><p>Save a property to see it here.</p><a className="btn" href="/explore">Explore stays →</a></div>}</section><section className="section compact"><h2>Recently Viewed</h2>{recentlyViewed.length?<div className="property-grid">{recentlyViewed.map(p=><PropertyCard key={p.id} property={p}/>)}</div>:<p className="muted">Properties you open will appear here.</p>}</section></main></>}